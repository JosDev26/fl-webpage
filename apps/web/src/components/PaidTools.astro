---
import '../styles/paidtools.css';
---
<section class="paidtools-container">
    <div class="paidtools-content">
        <h2 class="paidtools-title hidden-initial">
            ACCESO EXCLUSIVO: HERRAMIENTAS PARA MIEMBROS
        </h2>

        <div class="paidtools-grid">
            <a href="#" class="paidtool-card hidden-initial">
                <div class="paidtool-icon">
                    <i class="ph ph-calculator"></i>
                </div>
                <span class="paidtool-name">Calculadora de traspasos</span>
                <span class="paidtool-desc">Calcula al instante los costos totales por traspasar una propiedad o vehículo. Obtén un estimado claro de impuestos, timbres y honorarios notariales para planificar sin sorpresas.</span>
            </a>
            <a href="#" class="paidtool-card hidden-initial">
                <div class="paidtool-icon">
                    <i class="ph ph-device-mobile"></i>
                </div>
                <span class="paidtool-name">FULECR</span>
                <span class="paidtool-desc">Supervisa el avance de tu caso en tiempo real y mantén el control exacto de tus pagos y horas facturadas. Una herramienta diseñada para garantizar total transparencia durante todo tu proceso legal.</span>
            </a>
            <a href="#" class="paidtool-card hidden-initial">
                <div class="paidtool-icon">
                    <i class="ph ph-hand-coins"></i>
                </div>
                <span class="paidtool-name">Asesoría de Impuestos</span>
                <span class="paidtool-desc">Optimiza la carga tributaria de tu negocio con una planificación integral. Te asesoramos para cumplir con la ley mientras maximizas tus beneficios fiscales de forma segura.</span>
            </a>
            <a href="#" class="paidtool-card hidden-initial">
                <div class="paidtool-icon">
                    <i class="ph ph-question"></i>
                </div>
                <span class="paidtool-name">Banco de Preguntas y Respuestas</span>
                <span class="paidtool-desc">Evita confusiones legales costosas, como mezclar teletrabajo con servicios profesionales. Accede a nuestra base de conocimiento para resolver tus dudas laborales y comerciales al instante.</span>
            </a>
        </div>

        <a href="/login" class="paidtools-cta hidden-initial">Acceder</a>
    </div>
</section>

<script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/3.2.1/anime.min.js" is:inline></script>
<script>
    declare const anime: any;

    document.addEventListener('DOMContentLoaded', () => {
        const section = document.querySelector('.paidtools-container');
        if (!section) return;

        const isRestoring = sessionStorage.getItem('isRestoring') === 'true';
        const delay = isRestoring ? 150 : 0;

        setTimeout(() => {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        anime.timeline({ easing: 'easeOutCubic' })
                            .add({
                                targets: '.paidtools-title',
                                opacity: [0, 1],
                                translateY: [30, 0],
                                duration: 600,
                            })
                            .add({
                                targets: '.paidtool-card',
                                opacity: [0, 1],
                                translateY: [50, 0],
                                scale: [0.95, 1],
                                duration: 700,
                                delay: anime.stagger(150),
                            }, '-=300')
                            .add({
                                targets: '.paidtools-cta',
                                opacity: [0, 1],
                                translateY: [20, 0],
                                duration: 500,
                            }, '-=200');
                        observer.disconnect();
                    }
                });
            }, { threshold: 0.2 });

            observer.observe(section);
        }, delay);
    });
</script>
