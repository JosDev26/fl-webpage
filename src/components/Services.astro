---
import '../styles/services.css';
---
<section class="services-container">
    <div class="services-content">
        <h2 class="services-title hidden-initial">
            LO LEGAL NO TIENE QUE SER COMPLICADO,<br>
            SOLO <span class="highlight">BIEN HECHO.</span>
        </h2>
        
        <p class="services-subtitle hidden-initial">
            Empresa nueva, en marcha o una persona que necesita respaldo. 
            <span class="underline-text">Hacé clic en la opción que mejor describa tu situación.</span>
        </p>
        
        <div class="services-grid">
            <a href="#negocio-nuevo" class="service-card hidden-initial" tabindex="0">
                <div class="service-icon">
                    <i class="ph ph-rocket-launch"></i>
                </div>
                <span class="service-text">Estoy iniciando un negocio o empresa</span>
            </a>
            
            <a href="#empresa-funcionando" class="service-card hidden-initial" tabindex="0">
                <div class="service-icon">
                    <i class="ph ph-buildings"></i>
                </div>
                <span class="service-text">Ya tengo una empresa funcionando</span>
            </a>
            
            <a href="#consulta-personal" class="service-card service-card-wide hidden-initial" tabindex="0">
                <div class="service-icon">
                    <i class="ph ph-user"></i>
                </div>
                <span class="service-text">Soy una persona con una consulta legal</span>
            </a>
        </div>
    </div>
</section>

<script>
    declare const anime: any;
    
    document.addEventListener('DOMContentLoaded', () => {
        const servicesTitle = document.querySelector('.services-title');
        const servicesSubtitle = document.querySelector('.services-subtitle');
        const serviceCards = document.querySelectorAll('.service-card');
        
        if (!servicesTitle) return;
        
        const isRestoring = sessionStorage.getItem('isRestoring') === 'true';
        const delay = isRestoring ? 150 : 0;
        
        setTimeout(() => {
            // Observer para el título
            const titleObserver = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        anime({
                            targets: '.services-title',
                            opacity: [0, 1],
                            translateY: [-35, 0],
                            duration: 700,
                            easing: 'easeOutCubic',
                        });
                        
                        // Animar el subtítulo después
                        anime({
                            targets: '.services-subtitle',
                            opacity: [0, 1],
                            translateY: [20, 0],
                            duration: 600,
                            delay: 200,
                            easing: 'easeOutCubic',
                        });
                        
                        titleObserver.disconnect();
                    }
                });
            }, { threshold: 0.5 });
            
            titleObserver.observe(servicesTitle);
            
            // Observer para las tarjetas
            serviceCards.forEach((card, index) => {
                const cardObserver = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            anime({
                                targets: entry.target,
                                opacity: [0, 1],
                                translateY: [30, 0],
                                scale: [0.95, 1],
                                duration: 600,
                                delay: 300 + (index * 120),
                                easing: 'easeOutCubic',
                            });
                            
                            // Animar el icono
                            anime({
                                targets: entry.target.querySelector('.service-icon'),
                                scale: [0.8, 1],
                                rotate: ['-10deg', '0deg'],
                                duration: 500,
                                delay: 450 + (index * 120),
                                easing: 'easeOutBack',
                            });
                            
                            cardObserver.disconnect();
                        }
                    });
                }, { threshold: 0.3 });
                
                cardObserver.observe(card);
            });
        }, delay);
    });
</script>
